/**
 * Lazy-loaded component exports
 * 
 * Use these components instead of directly importing heavy libraries
 * to benefit from code-splitting and reduce initial bundle size.
 */

export { LazyModel3DViewer } from "./LazyModel3DViewer";
export { 
  LazyLineChart, 
  LazyAreaChart, 
  LazyBarChart, 
  LazyPieChart,
  LazyRadarChart,
} from "./LazyCharts";
