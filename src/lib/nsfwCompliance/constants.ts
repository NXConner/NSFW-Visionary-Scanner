export const PERFORMER_ROLES = ["performer", "instructor", "assistant", "other"] as const;

export const CONSENT_STATUSES = ["pending", "verified", "revoked", "expired"] as const;

export const RECORD_STATUSES = ["pending", "verified", "expired", "revoked"] as const;
