# Dependencies
node_modules/
.npm

# Test outputs
coverage/
playwright-report/
test-results/

# IDE
.idea/
*.iml
.vscode/

# Environment (local-only; never commit real secrets)
.env
.env.*
!.env.example
!.env.production

# Vite local overrides
.env.local
.env.*.local

# Firebase / Google Services (local-only)
android/app/google-services.json
ios/App/App/GoogleService-Info.plist

# Android signing / keystores (local-only)
android/keystore.properties
android/gradle.properties
android/app/release.keystore
**/*.jks
**/*.keystore

# Local archive workspace (do not commit temporary platform snapshots)
# Allow archived source diffs under `deleted files/src/**` to be committed when needed.
deleted files/*
!deleted files/src/
!deleted files/src/**
deleted files/no-longer-needed/ios-archive-2026-02-12/

# Supabase CLI temporary state
supabase/.temp/
